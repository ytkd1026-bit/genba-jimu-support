統合修正 — 残TODO
==================

■ 準備中（型・保存層は実装済み、入力画面のみ未実装）
- 08 案件ログ（projectLogs.ts 実装済み）… /projects/[projectId]/logs は準備中ページ
- 09 学び（learningRecords.ts 実装済み）… /projects/[projectId]/learning は準備中ページ

■ 見積・請求の発展余地
- 版管理は簡易版（REV-...-EST-01 / -INV-01）。改訂で版番号を増やす完全な履歴管理は未実装。
- 案件請求のお支払期限は空（PDFは「別途ご相談」表示）。入力欄の追加余地あり。
- 案件見積画面は WorkItem の閲覧＋保存＋PDF。明細の直接編集は「04 工事項目・原価」で行う設計。
- 一括請求（sample/invoice）は projectId 連携未対応（元請単位のデモ主体のまま）。
  個別請求は案件連携済み。一括請求の案件連携は次段階。

■ 既存見積画面（sample/estimate）
- 経路A（案件未選択の手入力）として残置。見積番号・作成日は従来サンプル値のまま
  （EstimatePDF は props 対応済みのため、画面から実値を渡す改修余地あり）。
- getSelectedEstimateId を読み込まない既存の不完全導線（register→estimate）は本タスク範囲外。

■ 会社設定
- settings/company の read は自キー直接（正本の書き手のため）。完全共通化するなら
  getCompanySettings/getBankSettings 経由に寄せる余地（accountType の型調整が必要）。

■ 複数タブ/端末
- localStorage 採番のため複数端末・同時編集には未対応。コード内 TODO 記載済み
  （idGenerator.ts / projectMigration.ts）:
    「TODO: Supabase移行時はDB採番またはUUIDへ変更する。
     localStorage採番は複数端末・同時編集に対応しない。」

■ 写真保存
- 圧縮済み（長辺1280px・JPEG0.72）でも localStorage 約5MB 上限。容量超過は
  エラー表示し他データを巻き込まない実装。多枚数運用時は IndexedDB 移行が必要。

■ 将来スコープ（今回対象外）
- Supabase接続 / ログイン / 権限 / 音声認識API / 保険会社API / 経営ダッシュボード

■ 税区分・税率対応で残したTODO（コード内に明記）
- 既存の単体見積画面（/projects/sample/estimate）は手入力フロー（LineItem に税区分なし）
  のため課税10%固定のまま。税区分UIは未対応（案件見積 /projects/[projectId]/estimate は対応済み）。
- 単体請求画面（/projects/sample/single-invoice）も INVOICE_LINES を課税10%固定で計算。
- 一括請求（/projects/sample/invoice）は案件単位集計で明細税区分は未対応。
  → いずれも案件フロー（WorkItem＋共通 calculateTaxBreakdown）で税区分対応済み。
     単体・一括フローの税区分対応は次段階。

■ マージ前最終修正で残したTODO
- 案件ステータス自動更新は主要工程のみ自動化：
  工事項目保存→estimating / 見積PDF→submitted / 請求PDF→invoiced / 作業報告→in_progress。
  approved（受注）・scheduled（施工予定）・completed（完工）・paid（入金）は
  案件詳細のステータス選択で手動更新（入金管理画面は今回対象外）。前進のみで後退しない。
- 見積の版管理は projectId 単位。previousEstimateId で前版へ連結（線形履歴）。
  版の削除UI・差分表示は未実装（過去版は保持のみ）。
- 請求書は案件あたり1件（版管理なし）。スナップショットで発行時点を固定。
  複数回請求（分割請求）を版として持つ設計は次段階。
- PDF目視は 見積明細書/請求書/既存見積明細書 を実施。発注確認書・保存用・
  現地調査報告書・写真台帳・作業報告書は生成成功（%PDF検証）まで。個別の画像目視は
  この環境にPDFレンダラ導入が重いため一部未実施（14-pdf-regression-check.txt に明記）。
